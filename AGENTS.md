# Agent Instructions for EPIONARA

## Git Synchronization Workflow
1. **Always Pull at Start**: Whenever starting work or receiving a new task/conversation, always run `git pull origin main` to sync the latest changes from GitHub.
2. **Always Push After Changes**: Whenever completing changes, fixes, or additions (after tests and verification), always commit and run `git push origin main` to sync with the remote repository.
